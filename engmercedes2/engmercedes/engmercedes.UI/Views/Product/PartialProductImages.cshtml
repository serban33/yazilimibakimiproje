
@model IEnumerable<engmercedes.UI.Models.UrunResimModel>

@foreach (var item in Model)
{
    <figure class="pro-thumb-item" data-mfp-src="data:image;base64,@Convert.ToBase64String(@item.RESIM)">
        <img src="data:image;base64,@Convert.ToBase64String(@item.RESIM)" alt="" />
    </figure>
}
