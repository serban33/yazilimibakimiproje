@model IEnumerable<engmercedes.UI.Models.UrunResimModel>

@foreach (var item in Model)
{
    <figure class="pro-thumb-item">
        <img src="data:image;base64,@Convert.ToBase64String(@item.RESIM)" alt="@item.RESIMAD" />
    </figure>
}

 
